# ShutaNote GPU 画布重写计划

## 目标

将当前基于 WPF `InkCanvas`、截图和 `BlurEffect` 的画布，逐步重写为：

```text
WPF 外壳与控件
    +
Direct3D 11 / Direct2D 画布渲染
    +
Windows Composition 玻璃与 GPU 模糊
```

保留现有白板 JSON 数据格式、主题设置、白板管理、工具栏和窗口行为。

## 约束

- 目标框架保持 `.NET 8 WPF` 和 `win-x64`。
- 不一次性删除现有 `InkCanvas`，每个阶段都必须可构建、可启动、可回退。
- 先抽离文档模型和坐标变换，再迁移渲染，避免数据层继续依赖 WPF 控件。
- GPU 不可用或初始化失败时，自动保留 WPF 画布作为回退。
- 每次修改后执行错误检查、构建并启动应用；启动测试完成后关闭进程。

## 阶段

### Phase 0：基线与安全网

- [x] 保存当前 WPF 版本行为。
- [x] 清理截图式玻璃刷新、`VisualBrush` 递归采样和窗口内 `BlurEffect` 方案的耦合。
- [x] 增加 GPU 渲染开关和失败回退开关。
- [x] 确认现有 JSON 文件可继续读取和保存。

### Phase 1：抽离画布文档模型

- [x] 新建与 WPF 无关的 `CanvasDocument`、`CanvasStroke`、`CanvasElement` 模型。
- [x] 将现有 `StrokeData`、`ElementData` 转换为统一模型。
- [x] 保留旧 JSON 字段和兼容读取逻辑。
- [x] 实现文档模型到 WPF 画布的适配器，确保现有功能不回归。

### Phase 2：GPU 基础设施

- [x] 引入并锁定兼容 .NET 8 的 Vortice Direct3D/Direct2D/DXGI 包版本。
- [x] 通过 Windows DirectWrite COM 工厂完成文本渲染基础设施探测。
- [x] 创建 `GpuDeviceManager`，完成 D3D11 硬件探测和失败状态管理。
- [x] 完善 D3D11 设备丢失重建。
- [x] 创建 WPF 承载层，使用 `D3DImage` 与 D3D9Ex 共享表面，未引入独立 HWND。
- [x] 创建测试 GPU 表面并绘制基础背景与后端、缩放和文档数量诊断信息。
- [x] GPU 初始化失败时切换回 WPF 适配器。

### Phase 3：Direct2D 画布渲染

- [x] 实现视口变换服务并接入 WPF 输入平移和缩放。
- [x] 将文档网格和基础对象绘制提交到 Direct2D 共享纹理目标。
- [x] 实现 Direct2D 抗锯齿笔迹折线和圆形线帽。
- [x] 实现 Direct2D 矩形、椭圆、箭头、图片及对象旋转。
- [x] 使用 DirectWrite 绘制文本；编辑时临时显示 WPF `TextBox`。
- [ ] 实现原始画布纹理与低分辨率模糊输入纹理。

### Phase 4：Composition 玻璃层

- [x] 创建 Windows Composition 兼容的窗口视觉层和画布承载桥接。
- [x] 为侧栏、标题栏、工具栏和状态栏保留独立半透明玻璃视觉层。
- [x] 移除 `RenderTargetBitmap` 定时截图；GPU 模糊效果仍待 Composition 视觉树接入。
- [x] 透明度滑条控制材质 Alpha，模糊度滑条即时更新窗口合成材质。
- [x] 保留浅色、深色、主题色和无 GPU 回退效果。

### Phase 5：输入、选择和编辑迁移

- [x] 将鼠标/触控坐标转换到文档坐标。
- [x] 通过透明 WPF 输入适配层保留选择、框选、拖动、旋转和中键平移。
- [x] 保留右键菜单、复制、粘贴、删除、全选。
- [x] 实现 GPU 元素命中测试。
- [x] 完成撤销/重做与自动保存后的 GPU 重绘接入。

### Phase 6：切换与移除旧实现

- [x] 增加设置项：GPU 画布、软件回退、诊断信息。
- [x] 默认启用 GPU；初始化失败自动回退。
- [ ] 对比导入、导出、缩放、旋转、绘制、白板切换等功能。
- [x] 删除截图计时器、`CroppedBitmap`、`VisualBrush` 和旧模糊代码。
- [ ] 更新 README、启动脚本和故障排查说明。

### Phase 7：低延迟交互改造

- [x] 移除持续触发的 `LayoutUpdated` 选择框刷新和工具栏软件阴影。
- [x] 绘画、图形创建、对象拖动和旋转期间使用 WPF 即时预览，操作结束后一次性提交 GPU 帧。
- [x] 保存与 GPU 提交复用同一份文档快照，并缓存图片编码，避免重复遍历和 PNG 编码。
- [x] 进入窗口移动/缩放循环时暂停亚克力和超大 `D3DImage` 呈现，结束后恢复。
- [x] 将 5000×3500 单一共享表面改为视口大小共享表面，按滚动偏移和缩放只提交可见区域。
- [x] 将自动保存改为后台串行写入和短时间防抖；窗口关闭前同步提交最新快照。
- [ ] 使用原始指针/触控数据与按帧合批的 Direct2D 增量笔迹，移除绘画阶段对 `InkCanvas` 的依赖。

## 验收标准

- 画布滚动、缩放、绘制和选择无明显输入延迟。
- 玻璃层只模糊画布纹理，不出现虚影、递归采样或扭曲。
- 修改模糊度时无需重新截图，效果即时更新。
- 旧版 JSON 白板可正常打开并保存。
- 没有 GPU 或初始化失败时应用仍可正常使用。
- Debug 构建成功，启动脚本退出码为 0。

## 当前执行顺序

1. 完成本计划和现状基线。
2. 先抽离文档模型及 WPF 适配器。
3. 再加入 GPU 设备管理和最小测试纹理。
4. 最后迁移画布与 Composition 玻璃，不直接破坏现有功能。
